const expect = require('expect')
const focalize = require('../src')

describe('Traversable#composeLens(lens)', () => {
  it('composing a lens with a traversable returns a traversable', () => {

    

  })
})
